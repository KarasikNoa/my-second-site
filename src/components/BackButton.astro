<button class="back-btn" onclick="history.back()" aria-label="חזור לעמוד הקודם">
  <span>←</span> חזור
</button>

<style>
  .back-btn {
    position: fixed;
    top: 1rem;
    right: 13rem;
    z-index: 100;
    background: var(--frc-red);
    color: white;
    border: none;
    border-radius: 999px;
    padding: 0.5rem 1.2rem;
    font-family: inherit;
    font-size: 0.95rem;
    font-weight: 500;
    cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,0.2);
    transition: background 0.15s ease, transform 0.15s ease;
  }
  .back-btn:hover {
    background: var(--frc-maroon);
    transform: translateY(-2px);
  }
  .back-btn span {
    display: inline-block;
    transform: scaleX(-1);
  }
</style>